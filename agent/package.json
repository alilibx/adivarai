{
  "name": "@adivari/agent",
  "version": "0.1.0",
  "description": "Adivari agent bridge — connect your coding agent's busy/idle state to the Adivari ad surface.",
  "bin": {
    "adivari": "dist/cli.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p .",
    "test": "node --test dist/detector.test.js",
    "start": "node dist/cli.js daemon"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.6.3"
  }
}
